'use client';

import ScheduleStoryPage from '@/app/(main)/(app)/(event)/events/[eventId]/story/schedule/PageClient';

export default function DemoScheduleStoryPage() {
    return <ScheduleStoryPage />;
}
