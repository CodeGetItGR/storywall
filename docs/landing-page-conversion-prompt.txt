Convert the attached exported HTML landing page (single file, e.g. "StoryWall-v152-*.html") into
production React/Next.js code inside the "storywall" Next.js 16 (App Router) repo, following ALL of the
conventions documented in docs/landing-page-nextjs-conversion.md in this repo. Read that file first — it
covers route placement, component/file structure, the hooks pattern for interactivity, Tailwind v4 (no
tailwind.config.js — CSS-based @theme tokens in app/globals.css), adding and using framer-motion for
animation, next-intl localization (messages/en.json + messages/el.json), and image handling. Also read
CLAUDE.md at the repo root for the project's general engineering conventions (thin pages, small components,
hooks-first, JSX section-boundary comments, no inline comments unless non-obvious, localize all copy).

Important context about the source file: it's a single HTML export that accumulated many incremental
patches (dozens of stacked <style> blocks and inline <script> blocks, each overriding earlier ones with
!important). Don't transcribe it block-by-block — resolve each element's actual final computed appearance
and behavior first, then implement that cleanly as Tailwind classes + framer-motion + small hooks. Inline
<script> blocks (mobile menu, filmstrip carousel, accordions, hero parallax, arrow navigation) should each
become a small hook under hooks/, not a <script> tag.

The page's text and images are still being finalized and will change after this conversion — so:
- Extract every visible string into next-intl translation keys under a new "LandingPage" namespace (see
  the guide for the expected shape), rather than hardcoding text in JSX. The source Greek copy becomes the
  el.json content; write real English translations for en.json, not placeholders.
- Keep components structured by section (hero, filmstrip, pricing, FAQ, etc.) so future copy/image swaps
  are localized to one small file instead of requiring a hunt through a large component.

Deliverable: the converted route + components + hooks + translation keys, matching this repo's existing
patterns closely enough that it needs no restructuring to merge — only content/image tweaks as the design
keeps evolving. Flag anything genuinely ambiguous (exact route slug, whether images go to public/ or stay
remote, any nav link that should point into the real app instead of an in-page anchor) instead of guessing.
